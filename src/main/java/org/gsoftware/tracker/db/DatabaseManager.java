package org.gsoftware.tracker.db;

/**
 * The DB manager, contains all the DB logic & functions
 * Uses singleton design pattern
 */
public class DatabaseManager {
    // TODO: Research singleton design pattern and how to embed into this.
    public static void getInstance() {

    }

    // TODO: Research how the data here will be needed to be used,
    //  most likely the data like saved applications will be needed here.
    /**
     * Function to log in user
     * @param username: The username to check db with
     * @param password: The password to check db with
     * @return Returns true if username and password are correct
     */
    public boolean login(String username, String password) {
        return false;
    }
}
